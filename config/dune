(executables
 (names discover_gtk discover_libintl)
 (modes (best exe))
 (libraries dune-configurator))

(rule
 (targets config.h config_intl.libs)
 (action (run ./discover_libintl.exe)))

(rule
 (targets config_gtk.cflags config_gtk.libs)
 (action (run ./discover_gtk.exe)))
